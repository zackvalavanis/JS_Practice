// // Make a hash to store a laptop's brand, model, and year. Then print each attribute on separate lines.


var hash = {brand: 'Apple', model: 'MacBook', year: 2020};
  console.log(hash.brand);
  console.log(hash.model);
  console.log(hash.year);


